"""Shared utilities package for cross-cutting modules.

This package is intended to contain code reused by both `controller` and
`runner` (and potentially other parts of the repository).
"""

__all__ = ["config"]
